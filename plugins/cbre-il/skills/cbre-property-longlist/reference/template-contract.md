# The template contract

The dashboard is produced by injecting three data blocks into a **frozen, versioned** template. The chrome (fonts, CSS, Leaflet, render JS) is byte-stable across every project; only data and a handful of config strings change. This is the whole value proposition - the CBRE look cannot drift.

## The template asset
`assets/dashboard_template.html` was originally generated from a reference dashboard by `helpers/make_template.py` (a HISTORICAL path now - see Versioning), which:
1. replaces the three data lines with markers: `/* @@INJECT:PROPS@@ */`, `/* @@INJECT:POIS@@ */`, `/* @@INJECT:REGIONS@@ */`.
2. replaces project-specific strings with `{{tokens}}`: `topbar_meta`, `eyebrow`, `title_html`, `footer_copyright`, and the KPI tokens (`kpi_properties`, `kpi_countries`, `kpi_regions`, `kpi_developers`, `kpi_wh_area`, `kpi_rent`, `kpi_countries_sub`, `kpi_regions_sub`, `kpi_wh_area_sub`, `kpi_rent_sub`). The authoritative token list is `_common.CONFIG_TOKENS`.
3. applies robustness `POST_PATCHES` (so a missing numeric shows `"tbd"` instead of crashing the render).
4. writes `assets/VERSION` (`<label>` + `chrome_sha256`).

Every replacement must hit exactly once or `make_template.py` aborts - the 11 MB file is never hand-edited blindly.

## Build = forward substitution (`build_dashboard.py`)
`render(canonical)` fills the `{{tokens}}` (hero strings from `meta.hero`; KPIs **computed from the data** so they cannot drift) and replaces the three markers with `const NAME = <minified-json>;`. It asserts no token or marker is left behind. The output is, by construction, the template plus exactly these substitutions.

## Byte-stability check (`gate_runner.py validate-html`)
Re-runs `render(canonical)` and asserts the delivered file is **byte-identical**, that the three blocks round-trip a JSON parse, and that the template's SHA matches `VERSION`. Any chrome change therefore fails the gate unless the template was deliberately re-versioned.

## Versioning
When a visual/template defect must be fixed (not a data fix), the template is **HAND-EDITED - that is the blessed, normal path**. `make_template.py`'s regenerate/patch machinery is HISTORICAL: the raw reference HTML is not on disk, and `--patch-only` applies only to the pre-i18n (v18) shape - which is no longer kept anywhere - so it can never rebuild the live template (v5-v18 were already hand-applied for the same reason).

**The hand-edit order is load-bearing** (skipping a step has cost real incidents):
1. take a dated copy of the whole skill folder FIRST (it is not a git repo - the copy is your only revert);
2. edit `assets/dashboard_template.html`;
3. bump `assets/VERSION` with the **CRLF-normalised TEXT hash** (`Path.read_text` - NOT the raw-byte hash `integrity.json` records; the two hashes differ on the same file, and putting the raw hash in `VERSION` blocks every run at exit 7);
4. `python helpers/make_integrity.py`;
5. run the evals - `smoke_test.py` (three blocks round-trip, byte-stable chrome), `i18n_test.py` (template<->EN key lockstep), `compare_test.py`/`flyover_test.py`/`numguard_test.py`/`areaunit_test.py` (the pinned per-version behaviours) - and land a NEW eval that pins your fix;
6. add the version's summary line below. `validate-html`'s SHA check then enforces the new version on every client run.

The version is pinned by `assets/VERSION` (label + `chrome_sha256`) and enforced by `validate-html`'s SHA check - not by any `project.yaml` key. The current template is **v45**, cumulative over the raw reference. (This summary lists v2-v18 + v25-v28 below; **v19-v24 are documented in the authoritative `make_template.py` `NOTE` blocks** - v19 i18n localisation, v20-v23 render-boundary/format, v24 Flyover view - not re-summarised here.) Summary:
- **v2** missing-numeric `fmt()` guard; **v3** comparison distance-row guards (empty POI type); **v4** visual fixes (card ref-chip spacing + `#` for the tofu numero sign, solid default map-marker fill so any country renders a readable number, transparent-on-dark compare-tray Clear button).
- **v5** client-side live nearest-POI discovery (`ensureNearestPois`/`rebuildPoiMarkers`/`populateMapPois` + hooks in `openModal`/`openCompare`/`switchView('map')`); offline = silent no-op. `POIS` stays `const` (mutated via `.push`), so no `build_dashboard`/gate-regex change.
- **v6** modal Workforce & Region guard (`reg`/`dist` null-safe + `rNum`/`rK`/`rStr` honest-`tbd` helpers + an honest "not yet added" placeholder).
- **v7** country FILTER options derive from `PROPS` via `Intl.DisplayNames` (the hardcoded HU/CZ/SK no longer stick on a new market); Site Plan toggle renders only when `p.plan` exists; coordinate-less properties no longer crash the modal.
- **v8** the "Open in Google Maps" anchor renders only when a real target exists (explicit `mapLink` -> else derived from coordinates -> else omitted; `href=""` used to reload the page).
- **v9** dataset-wide `FIELD_PRESENT` (a variable no input ever carried renders nowhere; partial fields still show TBC) + the Workforce block renders only value-bearing tiles (adds the Oxecon GDP-nominal/manufacturing/transport-storage tiles, drops the never-shipped employment-rate / GDP-per-capita-PPS tiles). Regions KPI sub-label is the static "Under consideration".
- **v10** unit-aware labels (source convention KEPT): `AREA_UNIT`/`RENT_PER` consts + the `{{kpi_wh_area_sub}}`/`{{kpi_rent_sub}}` tokens drive sq ft vs sq m and £/sq ft vs €/m² throughout the chrome.
- **v11** wage tile dropped for a derived logistics-employment-share tile (workforce now 100% dataset-sourced); reviewer twin regenerated on every freeze.
- **v12** per-property photo `gallery` (best-first, hero = `gallery[0]`, cap `images.GALLERY_MAX`) + a manual prev/next carousel on cards and in the modal, page-scoped so a multi-property deck never leaks a neighbour's photo.
- **v13** data-driven size slider (`initSizeSlider` sets bounds/step/default from the real area range, unit-aware label); single-country header (`adaptSingleCountryHeader` drops the Countries KPI tile, widens Regions); monthly + annual rent (`rentMonthlyStr`/`RENT_CUR`); the compare 'lowest rent' skips non-numeric rents.
- **v14** exhaustive total rent (GLA × rate) on the modal + compare: `totalAnnualRent`/`totalRentStr`, split warehouse@rate + office@rate when rates differ, derived `officeRentVal`/`officeAreaVal`.
- **v15** a tbd/absent modal description renders nothing (guarded by `isTbd`) instead of a bare 'tbd' paragraph.
- **v16** three area rows (`glaVal`/`glaStr` Total GLA = warehouse + office, plus warehouse + office components) on the modal + compare; a full-size photo lightbox (`#lightbox`/`#lb-img`/`openLightbox`) above the modal with prev/next + arrow-key/Esc handling.
- **v17** honest drive-time labelling driven by the build-time enrichment state via the single `{{dist_mode}}` config token (machine string `est`/`car`/`hgv`). JS maps it through `DIST_LABEL`/`DIST_BADGE` to a mode-aware Drive-time column header, an adjacent badge, an honest initial `distStatusHtml`, and a legend mode tag. The client-side live OSRM upgrade is kept (always CAR profile) and labelled honestly - it claims only live car-routed where reachable and discloses that truck/HGV times are not modelled, never claiming HGV; the header/badge reflect the build state. Defect 1 of the same change drops the baked km from the `enrich.py` POI notes so the grey sub-label carries provenance only (the single headline distance comes from the opened property's own coordinates).
- **v18** `landlord` surfaced as a party DISTINCT from the developer (the owner / asset manager / freeholder of an existing building): a `row('Landlord', p.landlord, 'landlord')` in the modal Availability & Site section and a `['Landlord', p=>p.landlord, 'landlord']` row in the compare table, EACH gated by `FIELD_PRESENT['landlord']` (the v9 machinery) so a no-landlord dataset renders no Landlord row anywhere. It is deliberately NOT on the card hero line, the filters, the search index or the sort (the card stays clean; developer remains the primary axis). `landlord` rides inside the `PROPS` data block - no new `{{config}}` token. Pairs with the data-engine change that stops `extract_xlsx` conflating landlord/owner/asset-manager into `developer`.
- **v25** (two chrome edits) - (1) a persistent **Compare** view: a fourth tab (after Grid/Map/Flyover) as a self-contained `<script>` IIFE before `</body>`, comparing ALL properties side-by-side by DEFAULT, honouring the live filters + sort, with per-property deselect chips (no cap). It REUSES the chrome's `compareHTML(items)` (the single N-agnostic renderer) so it can never drift from the popup; wraps `switchView`/`applyFilters` (origs preserved); keeps the toolbar visible; and pins the attribute column with `overflow-x` scroll for many properties (un-sticking the non-first header cells the base `.cmp-table th` rule sticks). The card tick-box popup (`state.compare` + its deliberate 4-property cap) is UNTOUCHED and independent. New i18n keys `tab_compare`/`cmp_tab_title`/`cmp_select_all`/`cmp_clear_all`/`cmp_shown_count`/`cmp_empty`/`cmp_no_match` (EN + 11 packs). Reads `PROPS` at runtime → byte-identity holds. Scale verified live at 4/8/12 properties. (2) **Flyover navigation**: scrolling no longer moves between options - the `IntersectionObserver` was removed and the slide panel shows ONE `.active` property at a time; navigation is prev/next buttons, arrow keys, **space bar**, and marker-click only (`flyover_hint` updated). Eval: `evals/compare_test.py` (+ `flyover_test.py`); JS execution/byte-identity via `modal_render_test`/`smoke_test`.
- **v26** `NUMOK` numeric guards across the chrome's numeric controls: a `tbd` can no longer top "Rent (lowest)", pass the size filter, win the Compare highlight, or kill the map. Eval: `evals/numguard_test.py`.
- **v27** system CJK font families (Simplified Chinese + pre-named TC/JP/KR) appended to the tail of both font stacks, so a non-Latin chrome renders without shipping fonts. Eval: `evals/i18n_test.py` (zh pack).
- **v30** the footer disclaimer no longer asserts a drive-time METHOD. It said "calculated from great-circle distance with a 1.25× road winding factor at a 75 km/h motorway average", which is FALSE on any routed run - the shipped numbers are real OSRM/openrouteservice road routes, and the G-honesty reviewer proved it arithmetically (implied speeds 59.6-85.4 km/h; legs both shorter and longer than the formula predicts). The footer is STATIC, so it made that claim regardless of `DIST_MODE`, while the honest per-mode basis was already stated by `DIST_BADGE` and the legend tag beside the numbers. The clause is DROPPED, not restated; "Drive times are indicative and for orientation only" survives because it is true in every mode. Applied in `helpers/i18n.py` (EN, the source of truth), the template's inline EN copy, and all 12 language packs - in each pack by cutting the method sub-clause and keeping that language's OWN existing orientation-only wording, so no new prose was authored or machine-translated into a client-facing disclaimer. Eval: `evals/footer_claim_test.py`.
- **v29** `LABEL_OVERRIDES = {epc:'EPC'}` so the auto-shown `epc` attribute reads **EPC** in the modal's Additional Details section instead of the `autoLabel`-derived "Epc". `epc` is a first-class field as of the B5 fix (BREEAM and EPC are different certificates; the tracker dictionary no longer aliases one to the other, and `merge._route_certifications` re-files a misfiled value), and it auto-shows because it is deliberately NOT in `DENY_FIELDS` - so this is a LABEL-only chrome edit, no new row, no new token, no `FIELD_PRESENT` gate needed. `breeam` keeps its curated Certification row; the two ship side by side. Eval: `evals/epc_breeam_test.py`.
- **v28** tbd-safe `areaStr()` across all seven area render sites (card, modal, compare, Flyover and the KPI strip) - a unit label can no longer ship glued to a `tbd` sentinel (`"tbd sq ft"`). Eval: `evals/areaunit_test.py`.
- **v31** `filterList`'s size filter compares an AREA-LESS property against `SIZE_MIN` (the slider's own floor) instead of the literal `0`, so a LAND/plot option (plotArea + landPrice, no warehouse) is visible AT REST and drops out only once the broker actively raises the slider. v26 wrote `f.size > 0`, correct while the slider started at 0; v13 then made the bounds DATA-DRIVEN, so `el.min === SIZE_MIN === ` the smallest real area and the resting `f.size` is already `> 0`. The two together hid every area-less option on FIRST LOAD - grid, "N shown" count and map - and since the slider cannot go below its own `min`, the card was unreachable by any interaction, not merely filtered. Caught live on a Spanish run whose KPI strip read 37 options over a 36-card grid (`render_qa.py` `cards=36 props=37`). v26's intent is preserved: an unknown area still fails an ACTIVE size filter. Eval: `evals/landoption_test.py` (extracts the branch from the template rather than restating it, so a revert to a literal fails); `evals/numguard_test.mjs` updated in lockstep.
- **v32** the two `officeArea` render sites (the modal row and the Compare row) format through a new `officeAreaStr(p)` companion. An independent G-visual reviewer found an office area shipping as a bare `1200` directly beneath a warehouse area reading `45,000 sq m` - no thousands separator, no unit - on ~25 of 37 properties in a live run, and traced it to those two lines passing `p.officeArea` raw while their `warehouseArea`/`plotArea` siblings on the adjacent lines went through `areaStr`. **Routing them through `areaStr` is NOT the fix and changes nothing visible**, which is the trap worth recording: `officeArea` is the one area canonical stores as a STRING (`officeAreaVal` is its numeric twin; `warehouseArea` and `plotArea` are numbers), and `areaStr` deliberately passes a string through untouched - correct for a `plotArea` reading `31.629 acres (12.8 ha)`, useless for a bare `"1200"`, so the string never reaches the numeric branch. `officeAreaStr` prefers `officeAreaVal` and falls back to the string whenever it carries any LETTER (a stated unit of its own, or the `tbd` sentinel), where appending `AREA_UNIT` would corrupt it - v28's exact caution. Verified in node: `"1200"`→`1,200 sq m`, `"1420.8"`→`1,420.8 sq m`, `"tbd"`→`tbd`, `"31.629 acres (12.8 ha)"`→unchanged, absent→`tbd`. Eval: `evals/areaunit_test.py`, whose site list is now the register of EVERY area render site and which pins each clause of the helper.

- **v33** PER-PROPERTY COMPLETENESS on the two surfaces that were still fixed lists. The rule: for each property individually the dashboard shows every variable that has a value for THAT property and renders no row for a variable it lacks (A with 10 filled fields shows 10 rows, B with 15 shows 15). The modal already complied (v21 omit-when-absent + v22 catch-all); these two did not. (1) **Flyover** `slideHtml` rendered a HARDCODED 9-row spec list, so sprinklers, permitting, rent-free, divisible-from, expansion, office rent and any brand-new key were invisible on every slide even while the modal displayed them; it now runs the modal's catch-all over the property's own keys, reusing the `DENY_FIELDS`/`autoLabel`/`LOCATOR_RE`/`isAbsent` script globals (same lexical scope as the `T`/`nfmt`/`AREA_UNIT` it already borrowed) and no-oping if a future chrome drops them. (2) **Compare**'s row list is hand-written, so a field the DATASET carries that nobody wrote a row for never appeared at all - `landPrice`, `incentives`, `reit` and `epc` were all in that position, as is any new scalar an interpretation record introduces (`meta.newFields`); it now appends `FIELD_PRESENT` fields that no curated row owns, sorted for determinism. Compare stays a MATRIX (union rows, per-cell unknowns) because a table cannot have per-column row sets; the v9 `FIELD_PRESENT` gate still drops a row no property carries. **Zero new i18n keys** (auto-labels ride the existing v22/v29 `autoLabel` + `LABEL_OVERRIDES`), so the EN/template lockstep is untouched. Pairs with the B58 capture fix upstream (`run.py` now hands the reader `_common.canonical_property_fields()`), because a display rule can only show what capture collected. Evals: `evals/percard_completeness_test.py` + `.mjs` (executes the real `detailHTML`/`compareHTML` in node:vm; structural pin for `slideHtml`, which is closed over inside the view IIFE).

- **v34** an APPROXIMATE pin is now visibly approximate ON THE MAP. The modal has rendered `modal_approx_note` since v6, but the map marker - the surface a broker actually scans - drew a town-centre geocode identically to a surveyed site, so a marker sitting in the middle of a village looked like fact. Two edits: the marker `divIcon` gains a `cm-approx` class when `p.coordsApprox` (`.cm.cm-approx{border-style:dashed;border-color:#ffd27a}`), and `propPopupHTML` renders the EXISTING `modal_approx_note` string under the meta line. **Zero new i18n keys** - `modal_approx_note` is already in EN + all 12 packs, so no client-facing prose was authored or machine-translated for this change (the v30 rule). Pairs with the B60 helper work that removes the *avoidable* approximations: `coords.dms_from_text` (exact DMS->decimal, the conversion nobody owned), `coords.coords_from_resolved` + `enrich.resolve_map_links` (follow a `maps.app.goo.gl` shortener to the author's own pin - preferring the `/maps/place/` destination over the `@` VIEWPORT centre, which sat ~190 m off on the live link), and the blocking `gate_runner.py coord-provenance`. Eval: `evals/coord_provenance_test.py`.

- **v35** the Flyover becomes a **SLIDE with a map on it** rather than a map with a caption. It shipped 70/30 map/details, which on a 1600px screen left the panel 403px - values broke mid-phrase (`€63 / sq m /` + `year`, `Eurovalley, a.s.` + `/ ReONE`), only about half a data-rich option's attributes sat above the fold, and on a greenfield site 70% of the frame was empty field carrying no information. Now **50/50**, with the details LEADING (`order:1`, so they sit left and the map follows) - the slide reading order, content first. The 671px panel pays for **three** spec columns with unwrapped values and a **two-column** drive-time grid whose heading spans both; a shared `--fo-pad` drives every gutter so they cannot drift per element; the hero height and the property name became `clamp()`s so a data-rich option keeps its rows above the fold. Responsive: two spec columns below 1100px (three would be ~110px each and wrap again), and below 900px the stack puts the MAP first - it orients you before the numbers - with the panel's divider flipping from `border-right` back to `border-top`. Verified live in Playwright on the 30-property build at 1600x1000 (map 50% / details 50%, 671px, 3 columns) after a broker reviewed a 70/30 vs 50/50 vs 40/60 preview and chose 50/50. **The frame is 88vh and the hero 17vh, both measured rather than chosen:** 80vh + a 22vh hero left all 30 options scrolling by up to 239px; 88vh + 17vh leaves 22/30 scrolling by at most 99px, and 8 fit outright. **Four spec columns was tried and is worse** (worst 164px, wrapped values 60 vs 31) - narrower columns wrap values, and a wrapped value is taller than the row it saved. A 20-attribute option cannot fit a fixed frame without shrinking the type, so the residual scroll is a disclosed trade; it stays inside the panel and never advances the property (v25). **Measure by activating each slide's `.active` class directly** - a keyboard/button sweep is unreliable, because on a slide whose panel scrolls, ArrowRight scrolls the panel instead of advancing and silently re-measures one slide (that artefact produced a false "zero overflow" reading twice during this change). Eval: `evals/flyover_slide_test.py`, which extracts the rules FROM the built template so a revert to 70/30 or a two-column spec grid fails.

- **v36** THE ROW SET IS FIXED AND HAND-AUTHORED. The chrome no longer derives any row from a property's own key set. Removed outright: the modal's catch-all and its `consumed` register (and the third argument of all 31 modal `row()` calls, which existed only to feed it), Compare's `curatedKeys`/`autoRows`, the Flyover's catch-all and `foCurated`, and the four script globals with no other consumer - `DENY_FIELDS`, `LABEL_OVERRIDES`, `autoLabel`, `LOCATOR_RE`. `FIELD_PRESENT` and `isAbsent` stay; both have other callers. This reverses the auto half of v33 and all of v21/v22/v29, and the reason is what those rows turned out to BE in practice: a reader following the open-schema rule invents a key per brochure, so one live longlist rendered 0 extra rows on its thinnest option and 47 on its richest, under machine-made labels, in an order nobody chose - `what3words`, `brochureDate`, `antiMoneyLaundering`, `particularsDate`. A field with no curated home still reaches the broker through the Source Ledger and the Longlist workbook, which is where an audit trail belongs; the dashboard shows what someone decided it should show. **Two rows were rescued rather than lost with the machinery.** (1) A new **Certification** row on all three views carries BREEAM *and* EPC, comma-separated: new globals `certName(v, name)` / `certStr(p)` beside `officeAreaStr` (before `detailHTML`, so all three views share the lexical scope), where `certName` prefixes a value with its certificate name unless the value already states it, keeping a leading Target/Targeting in front (`"Excellent"` -> `BREEAM Excellent`; `"Target A"` -> `Target EPC A`; `"Targeting BREEAM 'Outstanding'"` -> unchanged), and `certStr` returns `""` when neither exists so the row is omitted rather than showing a stray separator. This matters because `epc` had NO curated home anywhere - it reached the page solely through the catch-all - so removing the catch-all alone would have deleted EPC from the dashboard, which is exactly what happened on the CEVA run that prompted this, days after three overrides had been written to recover those very values from an over-tight plausibility gate. (2) Compare gains curated **Land price**, **Incentives** and **REIT** rows; v33's own note records that those three plus `epc` were never in its hand-written list, so they too would have gone. Compare and the modal now carry the same commercial set. **Zero new i18n keys** (reuses `cmp_certification`, `row_land_price`, `row_incentives`, `row_reit`), and `sec_additional` is dropped from `i18n.EN` and all 12 packs so the lockstep holds with nothing orphaned. Evals: `evals/certification_test.py` + `.mjs` execute the real `certName`/`certStr` in `node:vm`; `percard_completeness_test` is rewritten to pin BOTH halves of the new rule - a curated row omits when the property lacks it, and an uncurated scalar renders nowhere however real its value; `smoke_test`, `epc_breeam_test` and `modal_render_test` updated in lockstep.

- **v37** four broker-asked changes, three of them chrome. (1) The hero heading is **capitalised** - `hero_title_html` in `i18n.EN` now reads `Logistics <em>options</em> for your next facility.`; it is the i18n default rather than the template because the template carries only `{{title_html}}`, so a run whose `meta.hero.title_html` is blank picks the capitalised form up and a broker-authored heading still ships verbatim. (2) The **Developers KPI tile is gone** - the tile, the `kpi_developers` config token, its computation in `build_dashboard.compute_kpis`, and `kpi_developers_label`/`kpi_developers_sub` from `i18n.EN` and all 12 packs, so nothing is orphaned and the KPI strip is five tiles. `developer` remains a first-class field on the card hero line, the filters and the Compare table; only the header count went. `adaptSingleCountryHeader` selects tiles by their `data-i18n` label rather than by position, so dropping one cannot shift what it removes. (3) A **View brochure** link in the modal meta, beside Open in Google Maps, from a new canonical `brochureLink` field: declared in `canonical.schema.json` (so `canonical_property_fields()` registers it), bound from a tracker column by nine new `extract_xlsx` aliases (`brochure`, `online brochure link`, `particulars`, `listing url`, ...), and rendered through a new `modal_open_brochure` i18n key added to EN and all 12 packs. The link renders only when the property states one, so a dataset without brochure URLs is unchanged. (4) **`motorway` is condensed to a card-sized locator** by `normalize.short_motorway` (limit 40 chars), applied in `merge.canonicalize` so every source path gets it. Agents write a paragraph - one live tracker carried a 164-character sentence and another 119 - which pushed the card meta line onto three rows and overflowed the Compare cell. The road/junction/distance triples are pulled out in the order the source states them and rejoined while they fit: the 119-char example becomes `M5 J18/18A 2 miles; M49 J1 4.5 miles` and the 164-char one becomes `M5 J19 adjacent; M49 3 miles`. **Every token in the output is verbatim from the input** - no road, junction or distance is invented, reordered or rounded - and a value already inside the limit is returned untouched, so `M4, J17` is a no-op. Prose with no parseable road falls back to a word-boundary cut rather than a clever guess. Eval: `evals/header_brochure_motorway_test.py` pins all four, including the two real sentences byte-for-byte and the every-token-came-from-the-source property; `extract_test`'s KPI-dedup case now asserts the Developers KPI is absent.

- **v38** the reserved labour-market micro-profile object is renamed from `district` to `districtProfile`. `district` collided with the single most natural open-schema key a reader would pick for "the industrial estate / sub-area a park sits within" - `reference/interpretation.md`'s own live-generated `fields` array already advertised `district` as reader-fillable (it is derived from `canonical.schema.json`'s top-level keys), and `gate_runner.py`'s `PROV_ADVISE_FIELDS` already grouped `district` with `city`/`park`/`address`/`postcode` as a checkable plain string - but the schema typed top-level `district` as an OBJECT, so a reader that (correctly, per its own contract) wrote a plain estate name into it hard-blocked `validate-data` with "'X' is not of type 'object'". `district` is now a first-class STRING field (grouped with `city`/`park`/`postcode`); the renamed `districtProfile` object is otherwise unchanged (still orchestrator-filled only, never populated by the deterministic spine - nothing in `helpers/` writes to it). One template line moved: `p.district` -> `p.districtProfile` (the workforce panel's `const dist = ...` assignment); every `district`-named CSS class and i18n label (`district-panel`, `wf_district_label`, ...) is a UI label, not a JSON key, and is untouched. Eval: `evals/district_field_test.py`.

- **v39** a MULTI-COMPONENT `officeArea` renders as a **bold summary line over a real bulleted breakdown** in the detail modal. `officeArea` is the one area canonical stores as a STRING (v32), and for a property with several non-warehouse components it arrives as one run-on sentence - `"45,649 sq ft total non-warehouse area (offices, 2 levels 28,804; 2nd floor meeting room 1,711; transport office 1 7,492; transport office 2 7,427; gatehouse 215)"` - which rendered as a single unbroken paragraph. New display-only globals beside `officeAreaStr`: `oaSplitTop` (paren-depth-aware `;` split), `oaAreaLike`, `OA_ITEM`, `oaWithUnit`, `oaEsc`, `oaCap`, `oaParse`, `officeAreaHTML`, plus the `.oa-breakdown` list CSS. Three breakdown shapes are handled - a leading summary clause plus a parenthesised `;` list; a bare `;` list with no summary (`"Main Office 15,213 sq ft; Hub Office 3,975 sq ft; ..."`); and a `;` list whose items own their trailing qualifier (`"14,486 sq ft (ground floor); 15,592 sq ft (first floor)"`, which survives because the split is depth-aware). **The guard is what makes this safe:** the breakdown path is entered only when the string carries a LETTER - exactly the case where `officeAreaStr` already falls back to the string - so a bare number, an absent value and the `tbd` sentinel return `officeAreaStr(p)` byte-for-byte and the numeric twin can never be overridden. `officeAreaVal`, `glaVal` and the rent formulas are untouched; **this is display only**. Two deliberate design calls: (1) **Compare KEEPS the flattened string** - a five-item list in one matrix cell sets the row height for every column and destroys the side-by-side scan (v37's `short_motorway` reasoning); (2) magnitudes are **NOT re-formatted**, only the unit is appended, and only to a fragment with no letter of its own, so `"Main Office 15,213 sq ft"` can never collect a second unit (v28's caution, the 10.76x family) and `"1.711"` is never silently restated as `1,711`. A 1-2 digit tail is never read as an area, so `"transport office 2"` stays a NAME rather than becoming a 2 sq ft room. **Zero new i18n keys** - no prose was authored, so the EN/template lockstep is untouched. Eval: `evals/office_breakdown_test.py` + `.mjs` (executes the real `officeAreaHTML`/`detailHTML`/`compareHTML` in `node:vm`, including the escaped-vs-real-markup assertion the whole fix stands on); `areaunit_test`'s site register updated in lockstep, and `district_field_test`'s literal `v38` pin softened to `>= 38`.

- **v40** four broker-asked chrome changes, plus the data that two of them needed. (1) **The three streets basemaps move to a KEYLESS provider.** The previous tile host now bakes an "API KEY REQUIRED" watermark INTO the imagery it serves on its keyless endpoint, so that text reached the reader inside real map tiles on every dashboard this template builds - and nothing detected it, because the tiles still return success: no console error, no failed fetch, no blank tile, only a human looking at the picture, by which time the file is with the client. All three definitions (main map, modal mini-map, Flyover) now request `https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png`. Three details are load-bearing and are applied IDENTICALLY at all three sites, which is the part that stops them drifting apart again: `subdomains` is STATED as the THREE hosts a/b/c (the old definitions used four, so one request in four would have 404ed, and the Flyover's carried no `subdomains` key at all); the `{r}` retina placeholder is DROPPED, because the standard tile server serves no @2x variant and a retained `{r}` would 404 every tile on a high-DPI screen; and the attribution names the new provider only, since crediting a host that no longer serves the imagery is a false claim rather than a courtesy. The SATELLITE (Esri) layer beside each of the three is untouched, and zero references to the old host or its credit remain in the template or in a built dashboard. Eval: `evals/basemap_provider_test.py`, which extracts every `L.tileLayer` call FROM the file so a revert, a dropped `subdomains` or a re-added `{r}` fails. (2) **A card title identifies ONE OPTION, not one park**, via a new canonical `unit` field and a single `titleStr(p)` helper. There was no `unit` in the schema's 56 property keys, and every title and label site interpolated `p.park` ALONE, so two genuinely different units on one park rendered as two identical-looking cards - in the grid, the map popup, the map list, the modal, the Flyover slide and tooltip, and in BOTH sets of comparison chips, which is the exact moment the broker is being asked to choose between them. `unit` carries the source's own designator as printed ('Unit 3', 'Phase 2', 'Block A'), declared in `canonical.schema.json`, added to `_common.STRING_FIELDS` (so an unfilled one is the honest `tbd` and a tracker's bare `3` is coerced rather than hard-failing `validate-data`), bound by seven new `extract_xlsx` aliases behind a wide NEGATIVE veto, read from three explicit `extract_pdf` labels, and named in `reference/interpretation.md`'s reader contract. `titleStr` follows v28's single-point-of-truth pattern - **all nine** sites call it, not the six that were first counted (the map LIST item and the Compare VIEW's own chip `label()` are title sites too) - and it owes its callers three things: a SENTINEL is not a designator, so an absent unit renders the park name byte-for-byte with no trailing separator and no word for unknown; NO DOUBLE-PRINTING, so a park already named 'Kestrel Reach Unit 3' does not become 'Kestrel Reach Unit 3 Unit 3'; and a FALSY return when neither is stated, so the three sites that fall back to `T('label_option')` keep doing so. The de-duplication is a whole-TOKEN run over both sides reduced to alphanumeric words, deliberately NOT a substring test: a substring test suppresses a real unit `3` inside a park 'Harrier Court 300' and loses data to save a repetition. The join is a single SPACE, because the designator is part of how the source names the place and a middle dot in an `<h3>` reads as a breadcrumb of two facts. `unit` also joins the search haystack, since a designator the reader can SEE must be typeable. **Zero new i18n keys.** Eval: `evals/unittitle_test.py` + `.mjs` (executes the real `titleStr`/`cardHTML`/`propPopupHTML`/`detailHTML`/`compareHTML` in `node:vm`; structural pins for the five sites a sandbox cannot reach). (3) **The SOURCE'S OWN PRINTED TOTAL reaches the card.** `merge` has always lifted it into `canonical.meta.statedTotals`, and the arithmetic gate was its ONLY consumer - it reached neither the builder nor the chrome. Meanwhile the card's area figure is DERIVED (`glaVal` = warehouseArea + officeAreaVal), and a derivation reads BELOW the printed total whenever real space sits inside that total and in no summed field: mezzanine, ancillary, plant. The client saw a smaller building than the brochure states with nothing on the page to say so. `build_dashboard._attach_stated_totals` now passes the figure through on **`preBaked.statedTotal`** and a new `statedTotalHTML(p)` renders it as a `.rent-mo`-shaped sub-line inside the card's warehouse spec tile, beside the derived figure and never in place of it. Two calls are the whole safety of it: the container is `preBaked` (pipeline-assigned, already denied to readers, overrides, open capture and the workbook) rather than a top-level property field, because `extract_xlsx` records why - a top-level scalar is client-facing record surface and would print raw; and the threshold is **the arithmetic gate's OWN expression**, `max(50.0, 0.005 * total)`, quoted rather than re-invented, so the card and the gate can never disagree about whether a difference matters and agreement inside the tolerance renders nothing at all. Every skip mirrors the gate's own (no stated total, a total <= 0, a `warehouseArea` `glaVal` would refuse), and no unit reconciliation is attempted here for the same reason the gate attempts none: `merge.stated_total_for` already refuses to record an un-converted record. `glaVal`, `officeAreaVal` and the rent formulas are untouched; **this is display only**. The label reuses `row_total_gla`, so **zero new i18n keys**. Eval: `evals/statedtotal_card_test.py` + `.mjs`, which extracts the tolerance expression from BOTH `gate_runner.py` and `build_dashboard.py` and fails if they ever diverge - a quotation is only honest if something checks it. (4) **A SITE PLAN is no longer cropped.** The plan view reuses the hero `<img>` inside `.modal-hero`, a fixed 16/7 frame with `object-fit:cover` - right for a photograph, destructive for a plan, because a plan is the one image whose EDGES carry the information: yard depths, the plot boundary, dimension lines, the access road. Only the lightbox showed it whole, so the reader had to know to click. A `plan-mode` class on the frame switches to `object-fit:contain` over a flat neutral letterbox, TOGGLED (never added) at the ONE existing swap point - `bindImageToggle`'s `show()`, the function that already assigns `img.src` per mode - so the two cannot disagree about which mode is displayed and switching back restores the photo's cover fit. The photo hero rule and the 16/7 geometry are byte-identical to v39: the fix is the FIT, not the frame. Eval: `evals/planfit_test.py`.

- **v41** the chrome stops keeping its own vocabulary for "this value is absent", and one label stops carrying two derivations. (1) **`UNKNOWN_FORMS` is `normalize.UNKNOWN_FORMS` mirrored member for member (38 members), and `isAbsent` is the one reader of it.** It used to be a private six-member copy that lacked `n/a`, `tba`, `tbs` and every market phrase, so an auto-shown attribute a reader shipped as "TBA" rendered on the card as a real datum. The Python set is the MASTER: edit `normalize.UNKNOWN_FORMS` first, mirror it here, and `evals/f05_sentinel_parity_test.py` holds the two equal, member for member, so a divergence is a failing eval rather than a wrong card. Non-ASCII members are written as JSON escapes so the file stays ASCII. Three further hand-rolled JS absence tests (`detailHTML`'s `isTbd`, the Flyover's, and `certName`'s own `tbd`/`tbc` chain) now read the same list instead of each carrying a shorter one. **A stated `none` is deliberately NOT a member**, because the extraction contract names `"None"` as a stated negative that is DATA and never an absence; four Python consumers used to delete it and no longer do. The Python-side counterpart of this change also needed one narrowing the chrome does not: `normalize.looks_unknown_code` exempts the three family members that are also assigned ISO 3166-1 alpha-2 codes, because a two-letter COUNTRY CODE field reads those two characters as a country. That exemption is code-side only and does not reach the chrome. (2) **`glaVal` adopts the source's own stated total.** Since v40 the card could print `preBaked.statedTotal` beside the derived figure, but the MODAL kept computing warehouse plus office, so on a corpus whose decks state a GIA including ancillary space the card and the modal showed **different numbers under the same "Total GLA" label** - measured at 16,845 / 10,692 / 5,000 / 4,269 sq ft apart on four of nine live properties, with the bound site plans' own schedules confirming the CARD figure was the right one. `statedTotal()` is now the single reader of `preBaked.statedTotal` and `glaVal()` prefers it, falling back to warehouse plus office only where the source printed no total; `glaUnit()` carries the matching unit. Eval: `evals/f26_gla_single_derivation_test.py` + `.mjs`, which pins the single-derivation property so a later edit cannot quietly reintroduce two.

- **v42** a Total rent figure always says what area it was computed on. v41 let the Total GLA row carry the source's stated total, but `totalAnnualRent` kept multiplying the rate by the SUMMED warehouse plus office area, so a reader dividing the printed total rent by the printed GLA could get a rate that does not match the quoted one. The stated total may include a gatehouse, a plant room or a hub office that no one pays warehouse rent on, so computing rent on it would OVERSTATE; the fix is therefore not to change the money, it is to print the basis. **`rentBasis(p)` is the ONE place the rent inputs are qualified**, `totalAnnualRent()` multiplies exactly those components and reads no property field directly, and `rentBasisStr()` prints exactly those components and never their sum, so the three cannot drift. `totalRentHTML(p, monthly)` renders the figure with a `<small class="rent-basis">` line beside the ANNUAL figure only, since the monthly line is a restatement of the same basis and repeating it reads as a second fact. A component the property does not state is simply absent from the formula, **which IS the disclosure** rather than a guessed office. `rentBasis` is declared after `totalAnnualRent` on purpose: declarations hoist, and two evals read the functions in source order. **Zero new i18n keys.** Eval: `evals/f32_rent_basis_test.py` + `.mjs`.

- **v44** the three streets basemaps move OFF the OpenStreetMap standard tiles and onto Esri's keyless `World_Street_Map`. v40 moved them TO OSM to escape a provider that had started baking an "API KEY REQUIRED" watermark into its keyless imagery; OSM has now failed in the same shape, worse. Their [tile usage policy](https://operations.osmfoundation.org/policies/tiles/) requires a request the Foundation can attribute to a named application, and a dashboard this template builds cannot supply one: the file is opened from a `file://` path, so the request carries no usable `Referer`, and a page cannot set its own `User-Agent`. The server answers with a **403 whose BODY is an HTML "Access blocked" page**, and Leaflet paints that into the tile grid as a readable wall of text saying the app breaks the rules - on the client's screen, with no console error, no failed fetch and no blank tile, because the response SUCCEEDS. The block is also plausibly against the whole corporate egress IP rather than any one file, and an office of readers opening longlists is the "heavy use" the policy forbids outright, so this was never fixable by tuning the request. A KEYED OSM reseller (MapTiler, Stadia, Thunderforest, Geoapify) was rejected for one reason: the key would sit in plain text inside a file that gets emailed to clients, and key domain-locking cannot bind to a `file://` origin. Esri needs no key and already serves the SATELLITE layer beside each of these three, which is also what makes it a known quantity here rather than a fresh bet. Three details are applied IDENTICALLY at all three sites (main map, modal mini-map, Flyover), and each is pinned because each can fail silently: the path order is **`{z}/{y}/{x}`**, not the `{z}/{x}/{y}` of the OSM-style URL it replaces, because Esri's REST endpoint takes row before column and transposing them serves tiles of the wrong PLACE rather than an error; `subdomains` and the `{s}` placeholder v40 introduced are **DROPPED**, since Esri serves from one host and a retained `{s}` would request a subdomain that does not resolve; and the attribution is one **byte-identical** string at all three sites crediting Esri ALONE, because a credit naming OSM for imagery Esri serves is a false claim, and three drifting variants of a credit is how the v40 definitions came apart in the first place. The Flyover's URL is written as a full literal rather than `ESRI + path` like the hybrid layers beside it, specifically so the eval's extractor can see it. The satellite (`World_Imagery`) layers are UNTOUCHED and their attributions are byte-identical to v39. Eval: `evals/basemap_provider_test.py`, rewritten to extract every `L.tileLayer` call FROM the file, so a revert to any OSM host, a transposed `{z}/{x}/{y}`, a re-added `subdomains` or a drifted credit fails here instead of shipping. **Residual risk, stated because it cannot be tested:** this is the SECOND keyless provider to change its terms under this template, the failure mode both times returned HTTP success, and no automated check can read a watermark or a block page baked into imagery. The G-visual screenshot review is the only thing that can catch the third time, and it has to be a human looking at the picture.

- **v45** thirteen broker-asked changes, landed together because eleven of them are one of two seams. **SEAM A - ONE WORD FOR "NOT STATED".** The page carried four: the pipeline wrote `tbd` into every unfilled chrome-read field, the chrome's own fallbacks (`fmt`, `areaStr`, `glaStr`, the region tile's three helpers, five Compare cells) wrote `tbd` or a LONG DASH, three commercial rows printed a long dash through `|| '—'`, and `landPrice` had a long dash of its own written by `fill_render_sentinels`. A reader comparing two modals therefore saw three different words for the same fact, and the two dash spellings read as a value rather than an absence. `normalize.BLANK` is now the single owner (`TBC`), re-exported as `_common.BLANK` for the twenty modules that already import `_common`, and mirrored ONCE in the chrome as `const BLANK`. Three properties make the swap safe rather than sweeping: the token is a MEMBER of `UNKNOWN_FORMS`, so every reader that asks `looks_unknown()` still reads such a field as ABSENT and coverage, the trace gate and the Gaps Report are untouched; `tbd` REMAINS a recognised form, so a canonical built before v45, a broker's documented `expect: {"f": "tbd"}` and a reader's own wording all still resolve to absence; and `country` keeps `??` because it holds an ISO CODE, not prose. `build_dashboard` re-runs `fill_render_sentinels` over every property at render, so an OLD canonical rebuilt today ships the new token without a migration. Two literal comparisons that would have rotted silently were re-pointed at the family (`repairs`' cleared-value test, `run._prop_label`'s unit test). **SEAM B - WHAT THE CHROME NO LONGER SHOWS.** Four surfaces were removed, and a removal leaves debris in five places (the template, `CONFIG_TOKENS`, the EN table, the twelve bundled packs, and the data the builder injects), so each was followed through all five. (1) **The hero LEDE paragraph.** It restated the count the KPI strip states and described the filters sitting two rows below it, for a screenful above the first card. Gone with its `{{lede}}` token, its three CSS rules, its `hero_lede_fmt` key in EN and twelve packs, its `required` entry in the schema's hero object, and `_hero_copy`'s `{count}` self-heal; `cmd_i18n`'s advisory placeholder note follows the placeholder to `{client}` below. (2) **The BORDER POI category**, from the filter chips, `groupedDistances`, Location and Reach, the modal mini-map, Compare's nearest-border row, the live-POI radius caps and need list, and the Flyover's drive-time highlights. It is also filtered OUT OF THE INJECTED DATA by `build_dashboard._display_pois`, at the render boundary rather than in `enrich`: the border dataset, its selection and its coverage checks stay correct and stay tested, and re-enabling the category is a one-line change in each place. A POI with NO stated type is KEPT - dropping records on a missing field is how a filter becomes a silent data loss. The Flyover needed its own line because it reads `preBaked.distances` by NAME, not the `POIS` array, so the data filter cannot reach it; `cat()` still RECOGNISES a border crossing so a legacy entry is not mistaken for a city, and `driveHighlights` drops it. (3) **Compare's best-value highlight** and the note that explained it. A longlist is read across many attributes at once, and flagging two of them puts the dashboard's thumb on the scale of a decision that is the reader's; `minSizeIdx`, `minRentIdx`, the class application, both CSS rules and the `cmp_highlight_note` key are all gone. (4) **The POI layers start OFF** (`poiFilter: new Set()`), so the map opens on the properties rather than on twenty-two pins the reader did not ask for. **THE REST.** (5) **The headline names the OCCUPIER**: the EN default is now `{client} - Industrial &amp; Logistics <em>opportunities</em>`, filled by `.replace()` (never `.format()`, so a translator's stray brace degrades the headline rather than crashing the build), with the REGION deliberately absent because one longlist regularly spans two and a stated region the option set outgrows is worse than none. A blank `meta.client` drops the placeholder AND the separator in front of it, matched as a character CLASS rather than the EN ` - `, because a pack may join the halves its own way and the dangling-punctuation problem is identical in every language. An AUTHORED `title_html` is never touched. The eyebrow default reads `Property Longlist`, and the twelve packs keep their own headline wording with the placeholder prefixed rather than being re-translated. `_doc_title` now prefers an AUTHORED hero string over a resolved default, headline ahead of eyebrow within each, which is what lets v20's rule (the tab adapts to the project - a broker's Spanish eyebrow reaches the tab) and v45's (with nothing authored the tab names the client) both hold; the brand suffix joins with the chrome's own middle dot instead of a long dash. (6) **`displayName`**, the client's own name for an option as their tracker prints it ('Titan, Knowsley Business Park'), is a new canonical string field and `titleStr()` PREFERS it. Precedence, never replacement: it is absent on most datasets, and v40's park + unit rules (sentinel handling, whole-token de-duplication) still carry those. A single park holding two buildings was the original defect - two identical-looking cards - and a client tracker usually names them before any brochure does. (7) **The card names the PARTY**, labelled, via `partyLine()`: it printed a bare developer name beside the motorway, which read as a location fact and said nothing at all when the developer was unfilled. The LABEL FOLLOWS THE VALUE - `landlord` wins because it is the party to the lease, `developer` is the fallback under its OWN label, and neither stated prints `Landlord: TBC` - because the two are DISTINCT parties in this schema and a developer printed under a "Landlord" label is a false statement about who the reader would sign with. (8) **The FOUR RENT rows always print, BLANK included**, and they are the only rows on any surface that do. The rent is the number the reader came for: a vanished rent row reads as "rent does not apply here" rather than "nobody has quoted yet", and it makes two modals different SHAPES at exactly the four lines being compared. Every other row still omits when a source states nothing (`row()`, unchanged, so v21's rule governs the rest of the modal including the seven other commercial rows) - a missing service charge, land price or lease term is an ordinary unstated attribute, and printing eleven blanks to catch four would bury them. This replaced the whole-block version of the rule that v45 first shipped with; the three rows that used to print `|| '—'` are the ones that now print BLANK, which is the defect the rule was for. (9) **The modal's meta chips are sentinel-GUARDED** (`motorway`, `status`, `earlyAccess`, `breeam`), so a chip never reads `TBC` in a row of facts. (10) **Three more media links** - `videoLink`, `websiteLink`, `streetViewLink` - each its own chip beside the brochure, via one `linkChip()` helper that emits a chip ONLY for a stated `http(s)` URL. That guard is load-bearing rather than defensive: the sentinel a pipeline writes into an unfilled field is a STRING, so a truthiness test would have shipped `href="TBC"` as a live link on every property with no video. Nothing is composed from a name or from coordinates - a generated Street View link is a claim about what the camera shows. All four fields are declared in the schema with their rule, bound by NARROW `extract_xlsx` aliases (a column headed `link` alone stays UNBOUND rather than being guessed), added to `clarify.DISPLAY_FIELDS` (they are card slots, so a question about them is material), exempted from translation (a translated URL is a broken URL) and excluded from the locator gate's figure scan (a figure inside a URL is part of the URL). (11) **The availability field reads as a DATE**: `row_early_access`, `row_early_access_date` and `cmp_early_access` say "Available date" and the modal chip prefix says "Available", in EN. The canonical field is still `earlyAccess` and the twelve packs keep their own translations, so a non-English build still says its equivalent of "early access" until those are revised. **NOT in v45, and why:** the Property status KPI tile and the italic "construction not commenced" note both need a three-way status enum, and this schema has only free-text `status`; deriving the buckets in the chrome would put an untraceable classification on the card, so the tiles were left as they are pending a decision on a derived `cardStatus` field. Eval: `evals/chrome_v45_test.py` + `.mjs` - it holds the chrome's `BLANK` equal to `normalize.BLANK`, executes `titleStr`/`partyLine`/`cardHTML`/`detailHTML` in a node sandbox, and checks each removal in all five of its places. Fourteen existing evals pinned the old spelling or the old omission and were RE-POINTED at the owner of the value rather than loosened; `modal_render`'s and `percard_completeness`'s no-placeholder rules were SCOPED to the blocks where they still hold rather than deleted.

## Generalising to a new reference look
Point `make_template.py` at a new reference HTML and update the `CONFIG_REPLACEMENTS`/`POST_PATCHES` literals to match that file's exact strings. The data schema is shared, so only the chrome changes.
