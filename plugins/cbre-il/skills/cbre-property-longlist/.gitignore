# transient run artefacts and caches - never commit
evals/_out/
**/__pycache__/
*.pyc
.pytest_cache/
reference/geocode_cache.json
# keep (committed): SKILL.md, helpers/, reference/*.md, reference/ seed caches,
# templates/, the evals/ suite, and every file in assets/ - the template + VERSION +
# the i18n packs + the *.json / *.json.gz datasets and ledgers + placeholder.uri +
# reference_screenshots/. assets/integrity.json is the authoritative list of
# build-critical tracked files - do not let this comment drift from it.
